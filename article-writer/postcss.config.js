module.exports = {
  plugins: [
    require('@tailwindcss/postcss'), // Use the new Tailwind PostCSS plugin
    require('autoprefixer'),         // Optional, if you're using Autoprefixer
    // other plugins, if any...
  ],
};
